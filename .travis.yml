language: csharp
mono: latest
solution: receiptparser-net46.sln

install:
 - mono .paket/paket.bootstrapper.exe
 - mono .paket/paket.exe restore
 - nuget install NUnit.Runners -Version 3.5.0 -OutputDirectory testrunner

script:
  - xbuild /p:Configuration=Debug solution-name.sln
  - mono ./testrunner/NUnit.Runners.3.5.0/tools/nunit3-console.exe -v ReceiptParser.Test/bin/Debug/ReceiptParser.Test.dll